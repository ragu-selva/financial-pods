# Finalized Artifact Intake

Artifact intake is complete for the files discovered in `D:\Financial Ai studio` on 2026-08-16.

Original files remain unchanged at their source location. Canonical read-only copies are organized below `docs/reference-artifacts/` by category. Exact size and SHA-256 values are recorded in `ARTIFACT_CATALOG.md`.

## Intake rules

- Never edit a finalized DOCX, PDF, PPTX, HTML, image, or ZIP in place.
- Preserve original filenames and verify copies by SHA-256.
- Treat the extracted developer starter pack as reference material. Root execution files override similarly named files inside it.
- Add newly received artifacts to the catalog before using them.
- Temporary Office lock files beginning with `~$` are excluded.

## Categories

- `research/` — master strategy and research blueprint
- `specifications/` — combined V1 functional and technical specification
- `engineering/` — engineering/coding playbook and implementation assurance plan
- `growth/` — content growth and publishing strategy in DOCX/PDF
- `deck/` — strategy and implementation presentation
- `prototype/` — finalized golden-lesson HTML prototype
- `developer-agent-starter/` — extracted starter-pack reference tree
- `source-packages/` — unchanged ZIP packages
