# Finalized Artifact Intake Map

The referenced ChatGPT conversation identifies the following finalized/current deliverables, but its task interface does not expose their binary attachments to this Codex workspace.

Copy the original downloads without editing their contents:

1. Functional specification (DOCX) → `specs/`
2. Technical specification (DOCX) → `specs/`
3. Presentation deck (PPTX) → `docs/deck/`
4. Visual UI prototype (HTML/package and supporting images) → `prototype/`
5. Developer/agent starter pack (ZIP or extracted folder) → merge its existing files into the matching `architecture/`, `prompts/`, `skills/`, `scripts/`, `infra/`, `src/`, and `tests/` locations while preserving file contents and relative relationships.

Supporting images and render previews should be placed under `prototype/assets/` unless the original starter pack already defines their paths.

After intake, update `MANIFEST.md` with the exact original filenames, byte sizes, and SHA-256 checksums.
