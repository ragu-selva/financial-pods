# Integration tests

Sprint 00 integration coverage consists of API health tests, web smoke tests, and Docker service health checks. Cross-package integration suites begin only when corresponding product components are scheduled.
