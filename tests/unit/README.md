# Unit tests

Framework-specific unit tests are colocated with their owning application during Sprint 00. Shared deterministic domain tests will be added here when their packages are authorized in later sprints.
